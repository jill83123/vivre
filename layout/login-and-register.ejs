<!DOCTYPE html>
<html lang="zh-Hant-TW">
  <%- include('./layout/head', { title: isLogin ? '登入' : '註冊' }); -%>
  <body>
    <%- include('./layout/navbar'); -%>

    <main class="container flex-grow-1 d-flex flex-column justify-content-center py-15 py-lg-25">
      <div class="row justify-content-center">
        <div class="col-md-10 col-lg-8 col-xl-7 col-xxl-6">
          <div class="bg-white border border-primary-300 rounded-3 p-6 p-lg-10">
            <h1 class="fs-6 text-accent-200 mb-6 mb-lg-10"><%- isLogin ? '登入' : '註冊' -%></h1>

            <form action="#" class="needs-validation mb-6 mb-lg-10" novalidate>
              <div class="mb-4">
                <label for="userName" class="form-label fw-bold mb-3">會員帳號</label>
                <input
                  type="text"
                  class="form-control"
                  id="userName"
                  placeholder="請輸入會員帳號"
                  required
                />
              </div>
              <div class="mb-4">
                <label for="password" class="form-label fw-bold mb-3">密碼</label>
                <input
                  type="password"
                  class="form-control"
                  id="password"
                  placeholder="請輸入密碼"
                  required
                />
              </div>
              <% if(!isLogin) { %>
              <div class="mb-4">
                <label for="email" class="form-label fw-bold mb-3">電子信箱</label>
                <input
                  type="email"
                  class="form-control"
                  id="email"
                  placeholder="請輸入電子信箱"
                  required
                />
              </div>
              <% } %>
              <button
                type="submit"
                class="d-flex justify-content-center align-items-center gap-3 w-100 btn btn-accent-200 rounded-pill text-white fw-bold tracking-widest lh-md py-3 mt-6 mt-lg-10"
              >
                立即<%- isLogin ? '登入' : '註冊' -%><i class="bi bi-arrow-right fs-5"></i>
              </button>
            </form>

            <p class="other-login-ways text-gray-300 text-center mb-6 mb-lg-10">
              <span class="bg-white px-3">或以其他方式<%- isLogin ? '登入' : '註冊' -%></span>
            </p>

            <ul class="mb-6 mb-lg-10">
              <li class="mb-2">
                <button
                  type="button"
                  class="d-flex justify-content-center align-items-center gap-3 w-100 btn btn-gray-100 rounded-pill fw-bold tracking-widest lh-md py-3"
                >
                  <img src="../assets/images/icons-google.png" alt="google icon" />
                  使用 Google <%- isLogin ? '登入' : '註冊' -%>
                </button>
              </li>
              <li>
                <button
                  type="button"
                  class="d-flex justify-content-center align-items-center gap-3 w-100 btn btn-gray-100 rounded-pill fw-bold tracking-widest lh-md py-3"
                >
                  <img src="../assets/images/icons-facebook.png" alt="facebook icon" />
                  使用 Facebook <%- isLogin ? '登入' : '註冊' -%>
                </button>
              </li>
            </ul>

            <p class="text-center">
              <%- !isLogin ? '已經是會員了？' : '還不是會員嗎？' -%><a
                href="<%- isLogin ? 'register.html' : 'login.html' -%>"
                class="link-accent-200 ms-3"
                >點此<%- !isLogin ? '登入' : '註冊' -%></a
              >
            </p>
          </div>
        </div>
      </div>
    </main>

    <%- include('./layout/footer'); -%>
  </body>
</html>
